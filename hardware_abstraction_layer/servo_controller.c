#include <hardware_abstraction_layer/servo_controller.h>

// Initialize controller and underlying peripherals.
void servo_controller_open(servo_controller_t controller) {

}

// Deinitialize controller and underlying peripherals.
void servo_controller_close(servo_controller_t controller) {

}

// Adjusts PWM servo control signal to match passed position; the servo
// will try to go to and hold this position producing the necessary torques
// to do so.
void servo_controller_move_to(servo_controller_t position_degrees) {

}
